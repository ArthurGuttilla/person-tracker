{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "bun run build",
        "installCommand": "bun install"
      }
    }
  ]
}
